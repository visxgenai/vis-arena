# Vis Arena Web

React frontend shell for Vis Arena.

```bash
cp .env.example .env
pnpm install
pnpm dev
```

Vite loads `apps/web/.env` automatically. Set `VITE_API_URL` to point at the
backend. Defaults to `http://localhost:8000`.
