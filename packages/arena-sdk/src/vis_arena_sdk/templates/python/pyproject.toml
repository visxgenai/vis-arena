[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vis-arena-python-template"
version = "0.2.0"
description = "LLM template Vis Arena submission agent"
requires-python = ">=3.11"
dependencies = [
  "openai>=1.30.0",
  "playwright>=1.43.0"
]

[tool.setuptools]
py-modules = []
